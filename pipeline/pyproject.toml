[project]
name = "pipeline"
version = "0.1.0"
description = "Meeting protocol AI pipeline: STT, diarization, voiceprint, task extraction, summary"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "pydantic-settings>=2.4",
    "httpx>=0.27",
]

[project.optional-dependencies]
stt = [
    "faster-whisper==1.2.1",
    "sherpa-onnx==1.13.8",
    "sherpa-onnx-core==1.13.8", # explicit: macOS wheel omits the runtime dependency
    "numpy>=1.26",
]
# Heavy ML deps live here so backend can install the contract-only package.
ml = [
    "faster-whisper>=1.1",
    "sherpa-onnx==1.13.8",
    "sherpa-onnx-core==1.13.8",
    "pyannote.audio>=3.1",
    "speechbrain>=1.0",
    "httpx>=0.27",
    "numpy>=1.26",
]

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pipeline"]

[tool.ruff]
line-length = 100
target-version = "py312"
